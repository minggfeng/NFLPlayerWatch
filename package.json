{
  "name": "nflPlayerWatch",
  "version": "1.1.0",
  "description": "Crimes of the NFL",
  "author": "Jim Lee <jimleeisme@gmail.com>",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "test": "live-server --open=./index.html --port=8081",
    "build": "babel . --out-dir compiled --presets=es2015,react --ignore=node_modules,compiled --source-maps inline",
    "build:watch": "babel . --out-dir compiled --presets=es2015,react --ignore=node_modules,compiled --source-maps inline --watch"
  },
  "dependencies": {
    "bootstrap": "^4.0.0-alpha.2",
    "jquery": "^2.2.1",
    "lodash": "^4.6.1",
    "react": "^0.14.7",
    "react-dom": "^0.14.7"
  },
  "devDependencies": {
    "babel-cli": "^6.6.5",
    "babel-preset-es2015": "^6.6.0",
    "babel-preset-react": "^6.5.0"
  }
}